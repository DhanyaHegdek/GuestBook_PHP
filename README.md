# GuestBook_PHP

This project is a simple guestbook application built using raw PHP and MySQL. It allows users to submit their name and a message, which is stored in a database and displayed on the main page.
